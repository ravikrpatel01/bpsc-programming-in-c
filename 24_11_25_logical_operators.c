#include<stdio.h>

int main() {
    // int a, b;
    // printf("Enter two numbers: ");
    // scanf("%d %d", &a, &b);

    int flag = 0;

    // printf("Logical AND Result: %d\n", (a < b) && (b > 0));
    // printf("Logical OR Result: %d", (a > b) || (b > 0));
    printf("Logical NOT Result: %d", !flag);
    return 0;
}